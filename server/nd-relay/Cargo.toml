[package]
name = "nd-relay"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "Serveur de relais / TURN (analogue hbbr), tuyau chiffré aveugle à tickets signés. Voir plan 05/11."

[dependencies]
nd-proto = { workspace = true }
# Format des tickets de relais signés (Ed25519) : l'autorité vit dans nd-api,
# le relais ne fait que vérifier (plan 11).
nd-api = { path = "../nd-api" }

[lints]
workspace = true
