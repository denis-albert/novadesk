[package]
name = "nd-crypto"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "Session chiffrée de bout en bout (Noise), empreintes et SAS (voir plan 06)."

[dependencies]
nd-proto = { workspace = true }
# Noise Protocol Framework (handshake XX, AEAD ChaCha20-Poly1305) — voir plan 06.
snow = "0.9"

[lints]
workspace = true
